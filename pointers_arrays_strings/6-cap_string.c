#include <stdio.h>
#include "main.h"

/**
 * cap_string - capitalizes all words of a string
 *
 * return:0
 */

char *cap_string(char *s)
{
	int sep;

	if (sep = ',' || sep = ';' || sep = '.' || sep = '!' || sep = '?' || seop
}
