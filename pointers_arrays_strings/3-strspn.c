#include <stdio.h>
#include "main.h"

/**
 * _strspn - gets length of prefixed substring
 * @s: pointer to a character
 * @accept: pointer to a character
 *
 * Return:
 */

unsigned int _strspn(char *s, char *accept)
{
