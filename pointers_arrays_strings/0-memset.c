#include <stdio.h>
#include "main.h"

/**
 * _memset - memory with constant byte
 * @s: points to a character
 * @b: variable to a character
 * @n: variable to an unsigned integer
 *
 * Return:
 */

char *_memset(char *s, char b, unsigned int n)
{

