#include <stdio.h>

/**
 * mul- multiplies two integers
 * @a: variable of an integer that is the first factor
 * @b: variable of an integer that is the second factor
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);
}
